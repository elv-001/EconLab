[project]
name = "econ-sim"
version = "0.1.0"
description = "Rule-driven multi-agent economic simulator"
requires-python = ">=3.11"
dependencies = []

[project.scripts]
econ-sim = "econ_sim.run:main"

[tool.pytest.ini_options]
testpaths = ["tests"]
